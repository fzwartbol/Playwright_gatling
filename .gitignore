# Playwright (Java Maven project)
playwright/target/

# HAR files — generated at runtime, not committed
har/

# Gatling build output
gatling/target/

# Maven local repo cache
.m2/
*.class

# OS
.DS_Store
Thumbs.db

# WireMock recorded stubs — regenerated by the record task; only the static override is committed
wiremock/mappings/*.json
!wiremock/mappings/__auth-login.json

# IDE
.idea/
*.iml
.vscode/
